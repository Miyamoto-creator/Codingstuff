import numpy as np

def direction(a, b):
    return direction

def point_vs_vector(point, vector):
    point, vector = np.array(point), np.array(vector)
    direction_x = "Positive" if vector
        if
            return -1
        elif
            return 0
        else
            return 1
    return(point - vector)

point = (0, 1)
vector = [[0, 0], [1, 1]]
print(point_vs_vector(point, vector))

54993
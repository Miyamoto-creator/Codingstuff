counter = 1  # define counter variable
step = int(input())  # let it be 3  # attach step to user input
counter += step  # Adds a value and the variable and assigns the result to that variable.
print(counter)  # it should be 4, then
